rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Allow access only to specific email addresses
    match /{document=**} {
      allow read, write: if request.auth != null
        && request.auth.token.email in [
          "kaalm1@gmail.com",
          "yeshov@gmail.com",
          "joanne.tisch@gmail.com"
        ];
    }
  }
}
